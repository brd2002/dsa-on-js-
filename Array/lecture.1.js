/**
 * ! Array lecture 1
 * ? data structure is how data should be represented in memory
 * * Array actually store data in a linear contiguous orientation in the memory 
 * * Js array support hetrogenous data -> string , number , boolean can be stored
 * * String in js is immutable type 
 * * Array in js is mutable
 * ! lecture done 4 , 5 , 
 */
let x = [1, 2, 3, 4, 5 , "what is your name"];
let str = "bharat"

// console.log(x);
console.log(str.length);
